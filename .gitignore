__pycache__/
*.pyc

# Secrets — never commit. IBKR keys + account id live in GitHub Actions secrets.
.env
.env.*
*.pem
*.key
id_rsa
id_rsa.*

# Local build / server cruft
_site/
.DS_Store

# MetalMiner historical dumps are licensed — pass them to scripts by path, never commit.
# (Not *.json.gz: history/daily/*.json.gz is the committed snapshot archive.)
historical_*.json.gz
historical_*.json
